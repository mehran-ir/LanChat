# تاریخچه تغییرات LanChat by MGH

این فایل تمام تغییرات هر نسخه را ثبت می‌کند. از این به بعد، هر تغییری در کد،
شماره نسخه (در `version.py`) و یک بند جدید در همین فایل دریافت می‌کند.

قالب شماره‌گذاری: `MAJOR.MINOR.PATCH` (مثلاً 1.2.0)

---

## [1.6.2] — تنظیم رنگ‌های پیش‌فرض + کوچک‌تر کردن مربع خالی

- رنگ‌های پیش‌فرض دکمه‌ها روی همان مقادیری قفل شد که کاربر با Color Picker انتخاب کرده بود.
- مربع نشانگر رنگ زمینه (که آیکون ندارد و فقط رنگ را نشان می‌دهد) کوچک‌تر شد.

---

## [1.6.1] — اصلاحات ظاهری جزئی

- آیکون تغییر رنگ زمینه از دایره به مربع تغییر کرد.
- مربع رنگی دور آیکون ایموجی حذف شد (فقط خود آیکون نمایش داده می‌شود).
- با انتخاب تم تیره، پس‌زمینه‌ی باکس اصلی چت هم به‌طور خودکار تیره می‌شود.

---

## [1.6.0] — انتخاب رنگ دلخواه دکمه‌ها + کوچک‌تر شدن دکمه‌ها

- دکمه‌ی 🎨 جدید در بالای برنامه: پنجره‌ای باز می‌شود که خود کاربر با یک Color Picker
  واقعی، رنگ دلخواهش را برای هرکدام از سه دسته دکمه (عادی/لغو-حذف/ارسال) انتخاب می‌کند.
  این انتخاب ذخیره می‌شود و بین اجراهای بعدی برنامه هم می‌ماند.
- سایز همه‌ی دکمه‌ها (padding، فونت، شعاع گردی گوشه) کوچک‌تر و جمع‌وجورتر شد.

---

## [1.5.0] — رنگ دقیق دکمه‌ها طبق تصویر مرجع + گوشه گرد

- رنگ دکمه‌ها به دقیقاً همان سه رنگ تصویر مرجع کاربر تنظیم شد: آبی `#0067EC`
  (عملیات عادی)، بنفش `#B73EF8` (لغو/حذف)، سبز `#00B751` (فقط دکمه‌های ارسال).
- گوشه دکمه‌ها کمی گرد شد (با تصویر ۹-تکه‌ای Pillow، چون ttk گوشه گرد بومی ندارد).

---

## [1.4.0] — توازن رنگی دکمه‌ها

- پالت رنگی دکمه‌ها از ۸ رنگ مختلف به فقط **۳ رنگ هماهنگ** کاهش یافت تا ظاهر برنامه
  شلوغ و ناهماهنگ نباشد: فیروزه‌ای آرام (اکثر عملیات)، خاکستری ملایم (عملیات خنثی
  مثل بستن/لغو)، و قرمز فقط برای حذف/عملیات غیرقابل‌بازگشت.

---

## [1.3.0] — آیکون اختصاصی برنامه (لوگوی M Chat)

- لوگوی رسمی «M CHAT ON LAN» به‌عنوان آیکون برنامه در همه‌جا تنظیم شد:
  پنجره/نوار عنوان، آیکون خودِ فایل exe (در File Explorer و قبل از اجرا)، و آیکون Tray.
- فایل‌های آیکون در پوشه‌ی جدید `assets/` نگه‌داری می‌شوند (`icon.png` برای پنجره/Tray،
  `icon.ico` چندرزولوشنی برای exe) و در ساخت exe به‌طور خودکار همراه بسته می‌شوند.

---

## [1.2.0] — تکمیل جلوه کریستالی

- کل پنجره‌ی برنامه کمی شفاف شد (حس شیشه‌ای ملایم، فقط ویندوز/مک)
- یک نوار باریک گرادیان رنگی (فیروزه‌ای → بنفش کم‌رنگ → صورتی کم‌رنگ) بالای پنجره
  اضافه شد، شبیه بازتاب نور روی کریستال — تکمیل‌کننده‌ی حباب‌های شیشه‌ای نسخه قبل.

---

## [1.1.0] — جلوه کریستالی/شیشه‌ای برای حباب پیام‌ها

- حباب‌های پیام (متن و تصویر) حالا گوشه‌های گرد، گرادیان نرم عمودی، و یک جلوه‌ی
  براقی (Shine) بالای حباب دارند — حسی شیشه‌ای/کریستالی، شبیه طراحی‌های مدرن.
  رندر با Pillow انجام می‌شود؛ در نبود Pillow به‌طور خودکار به مستطیل ساده قبلی برمی‌گردد.

---

## [1.0.1] — نسخه‌بندی فایل خروجی

- شماره نسخه اکنون در نام فایل exe ساخته‌شده هم می‌آید (مثلاً `LanChat by MGH v1.0.1.exe`)
  تا هر بار دانلود، به‌وضوح مشخص باشد کدام نسخه است — هم در build.bat و هم در GitHub Actions.

---

## [1.0.0] — نسخه اولیه‌ی کامل و نسخه‌بندی‌شده

### امکانات اصلی شبکه و چت
- کشف خودکار کامپیوترهای شبکه با UDP Broadcast (چندبار تکرار + ارسال از تمام کارت‌های شبکه برای قابلیت‌اطمینان بالاتر)
- افزودن دستی کامپیوتر با IP
- ارسال/دریافت پیام متنی و فایل با هر حجم
- چت گروهی با مفهوم «ادمین گروه» (افزودن/حذف عضو فقط توسط ادمین، هم‌گام‌سازی خودکار برای همه اعضا)
- لغو ارسال پیام (پیش و پس از ارسال) و پاک کردن تاریخچه با تاییدیه دو مرحله‌ای
- پاسخ به پیام (Reply) با پیش‌نمایش پیام اصلی
- تیک خوانده‌شد (Read Receipt)
- تلاش خودکار مجدد برای پیام/فایل‌های ناموفق (وقتی طرف مقابل دوباره آنلاین شود)
- نمایش وضعیت آنلاین/آفلاین مخاطبان تکی

### رابط کاربری
- تم‌های رنگی قابل‌انتخاب + رنگ مستقل باکس چت
- ایموجی رنگی واقعی (فونت Segoe UI Emoji) در دکمه‌ها و پیام‌های فقط-ایموجی
- پیش‌نمایش تصویر داخل پیام (شبیه واتساپ) با پنجره بزرگ‌نمایی و دکمه «نمایش در پوشه»
- تصویر پس‌زمینه اختصاصی برای هر گفتگو (کپی دائمی در پوشه برنامه، حتی با اسکرول ثابت می‌ماند)
- تاریخ شمسی و ساعت تهران در گوشه بالای برنامه
- Badge شمارنده پیام‌های نخوانده روی آیکون Taskbar
- Tooltip روی دکمه‌های آیکونی
- جستجو در لیست کامپیوترها و در متن گفتگو
- رفع باگ نمایش/تایپ متن فارسی (ترتیب راست‌به‌چپ)

### پس‌زمینه و سیستم
- آیکون در Tray کنار ساعت ویندوز با گزینه باز کردن/خروج (دکمه X فقط مخفی می‌کند)
- نوتیفیکیشن داخلی قابل‌کلیک (کلیک → باز شدن برنامه و رفتن به گفتگوی مربوطه)
- بازر (Buzzer) با صدا و لرزش پنجره، همیشه نوتیفیکیشن می‌دهد حتی اگر نوتیفیکیشن‌ها خاموش باشند
- امکان تعیین نام نمایشی دلخواه (به‌جای نام سیستمی ویندوز) با قابلیت ویرایش بعدی
- ذخیره‌سازی دائمی لیست کامپیوترها/گروه‌ها/تاریخچه در `lanchat_data.json`
- اعتبارسنجی پیام‌های گروهی (فقط اعضای واقعی/ادمین واقعی می‌توانند پیام یا تغییرات معتبر بفرستند)

### ساخت و توزیع
- ساخت خودکار فایل exe با GitHub Actions (بدون نیاز به نصب پایتون روی سیستم کاربر)
- اسکریپت build.bat برای ساخت محلی

---

## راهنمای نسخه‌های بعدی
هر تغییر جدید باید:
1. شماره نسخه در `version.py` را طبق قوانین Semantic Versioning افزایش دهد.
2. یک بند جدید در بالای همین فایل (زیر یک تیتر `## [نسخه] — تاریخ`) اضافه کند که خلاصه‌ی تغییر را بنویسد.
